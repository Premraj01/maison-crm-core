import { createFileRoute, Link } from "@tanstack/react-router";import { Plus, ShieldCheck } from "lucide-react";import { useState } from "react";import { toast } from "sonner";import { can, roles, team } from "@/data/crm";import { useCrm } from "@/lib/crm-context";import { PageHeader,StatusPill } from "@/components/crm/Primitives";import { Avatar,AvatarFallback } from "@/components/ui/avatar";import { Button } from "@/components/ui/button";import { Card } from "@/components/ui/card";import { Dialog,DialogContent,DialogDescription,DialogFooter,DialogHeader,DialogTitle } from "@/components/ui/dialog";import { Input } from "@/components/ui/input";import { Label } from "@/components/ui/label";
export const Route=createFileRoute("/team/")({head:()=>({meta:[{title:"Team — Maison CRM"},{name:"description",content:"Maison team members and role assignments."},{property:"og:title",content:"Team — Maison CRM"},{property:"og:description",content:"Maison team members and role assignments."},{property:"og:type",content:"website"},{name:"twitter:card",content:"summary_large_image"}]}),component:TeamPage});function TeamPage(){const {role}=useCrm();const [open,setOpen]=useState(false);return <><PageHeader eyebrow="Workspace" title="Team" description="People with access to this Maison workspace." actions={<><Button asChild variant="outline"><Link to="/team/permissions"><ShieldCheck/>Permissions</Link></Button><Button onClick={()=>setOpen(true)} disabled={!can(role,"team:invite")}><Plus/>Invite user</Button></>}/><Card className="overflow-hidden"><div className="overflow-x-auto"><table className="w-full min-w-[650px] text-left"><thead className="border-b border-border bg-secondary/60 text-[10px] uppercase tracking-[.14em] text-muted-foreground"><tr><th className="px-5 py-3">User</th><th>Role</th><th>Status</th><th>Last active</th></tr></thead><tbody>{team.map((m,i)=><tr key={m.id} className="border-b border-border last:border-0"><td className="flex items-center gap-3 px-5 py-4"><Avatar><AvatarFallback>{m.initials}</AvatarFallback></Avatar><div><p className="text-sm font-semibold">{m.name}</p><p className="text-xs text-muted-foreground">{m.email}</p></div></td><td className="text-sm">{roles.find(r=>r.value===m.role)?.label}</td><td><StatusPill tone={m.active?"success":"neutral"}>{m.active?"Active":"Invited"}</StatusPill></td><td className="text-xs text-muted-foreground">{i===0?"Now":`${i+1}h ago`}</td></tr>)}</tbody></table></div></Card><Dialog open={open} onOpenChange={setOpen}><DialogContent><DialogHeader><DialogTitle className="font-display text-2xl">Invite a teammate</DialogTitle><DialogDescription>They will receive a sample invitation notification.</DialogDescription></DialogHeader><div className="my-6 space-y-4"><div className="space-y-2"><Label>Email</Label><Input type="email" placeholder="name@company.com"/></div><div className="space-y-2"><Label>Role</Label><select className="h-9 w-full rounded-md border border-input bg-background px-3 text-sm">{roles.map(r=><option key={r.value}>{r.label}</option>)}</select></div></div><DialogFooter><Button variant="ghost" onClick={()=>setOpen(false)}>Cancel</Button><Button onClick={()=>{setOpen(false);toast.success("Invitation prepared")}}>Send invitation</Button></DialogFooter></DialogContent></Dialog></>}
