import { createFileRoute, Link } from "@tanstack/react-router";import { ArrowLeft, Check, Minus } from "lucide-react";import { can,permissionRows,roles } from "@/data/crm";import { useCrm } from "@/lib/crm-context";import { PageHeader } from "@/components/crm/Primitives";import { Card } from "@/components/ui/card";
export const Route=createFileRoute("/team/permissions")({head:()=>({meta:[{title:"Permissions — Maison CRM"},{name:"description",content:"Role capability matrix for the Maison workspace."},{property:"og:title",content:"Permissions — Maison CRM"},{property:"og:description",content:"Role capability matrix for the Maison workspace."},{property:"og:type",content:"website"},{name:"twitter:card",content:"summary_large_image"}]}),component:Permissions});function Permissions(){const {role}=useCrm();return <><Link to="/team" className="mb-6 inline-flex items-center gap-2 text-xs text-muted-foreground"><ArrowLeft className="size-4"/>Team</Link><PageHeader eyebrow="Access control" title="Permissions" description="The highlighted column is the role currently being previewed."/><Card className="overflow-hidden"><div className="overflow-x-auto"><table className="w-full min-w-[650px] text-left"><thead><tr className="border-b border-border"><th className="p-5 font-display text-lg font-medium">Capability</th>{roles.map(r=><th key={r.value} className={`p-5 text-center text-xs ${role===r.value?"bg-secondary text-primary":"text-muted-foreground"}`}>{r.label}</th>)}</tr></thead><tbody>{permissionRows.map(p=><tr key={p.action} className="border-b border-border last:border-0"><td className="p-5 text-sm">{p.label}</td>{roles.map(r=><td key={r.value} className={`p-5 text-center ${role===r.value?"bg-secondary":""}`}>{can(r.value,p.action)?<Check className="mx-auto size-4 text-success"/>:<Minus className="mx-auto size-4 text-muted-foreground/40"/>}</td>)}</tr>)}</tbody></table></div></Card></>}
